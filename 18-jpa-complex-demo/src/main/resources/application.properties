#启动项目时创建表结构，结束项目时删除
spring.jpa.hibernate.ddl-auto=create-drop
#让hibernate打印每一条sql
spring.jpa.properties.hibernate.show_sql=true
#让hibernate打印每一条sql格式化输出
spring.jpa.properties.hibernate.format_sql=true
